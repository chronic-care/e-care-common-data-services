// README: This is only to convert CSV to JSON, to run this, change it to js and simply run node convertCsvToJson.js

// const fs = require('fs')
// const path = require('path')
// let csvToJson = require('convert-csv-to-json');

// const dirname = path.resolve('../resources');

// const files = fs.readdirSync(dirname)

// files.forEach((filename) => {
//   const filenameTrim = filename.replace('.csv', '');
//   csvToJson.fieldDelimiter(',').generateJsonFileFromCsv(dirname + '/' + filename, dirname + '/' + filenameTrim + '.json');
// })
